<div class = info >
	<%= render 'users/info', user: @user %>
</div>
<%= render 'books/new', book: @book %>

<table>
	<tr><th>title</th><th>Body</th></tr>
<% @books.each do |book| %>
<tr>
	<td><%= book.title %></td>
	<td><%= book.body %></td>
	
</tr>
<% end %>
</table>
