<div class = info >
	<%= render partial: "users/info" %>
</div>
<table>
	<tr><th>title</th><th>Body</th></tr>
<% @books.each do |book| %>
<tr>
	<td><%= book.title %></td>
	<td><%= book.body %></td>
	
</tr>
<% end %>
</table>
