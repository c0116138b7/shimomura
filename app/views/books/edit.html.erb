<h1>Editing Book</h1>

<%= form_for @book, url: update_path do |f| %>
<h4>title</h4>
	<%= f.text_field :title %>
<h4>Body</h4>
	<%= f.text_area :body %>
	<%= f.submit %>
<% end %>

<%=  link_to 'show', show_path(@book.id) %> <%=  link_to 'Back',  new_path%>