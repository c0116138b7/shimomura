<h1>Editing Book</h1>

<%= form_for @book do |f| %>
<h4>title</h4>
	<%= f.text_field :title %>
<h4>Body</h4>
	<%= f.text_area :body %>
	<%= f.submit %>
<% end %>

<%=  link_to 'show', book_path(@book.id) %> <%=  link_to 'Back',  books_path%>