<div class = info >
	<%= render partial: "info" %>
</div>
	<%= render partial: "books/new" %>


<h2>Books</h2>
<table>
	<tr><th></th><th>title</th><th>opinion</th></tr>
<% @books.each do |book| %>
<tr>
	<td></td>
<td><%= link_to book.title ,book_path(book.id) %></td>
<td><%= book.body %></td>
</tr>
<% end %>
</table>
